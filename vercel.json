{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"buildCommand": "pnpm validate-env && pnpm build && pnpm convex deploy",
	"ignoreCommand": "if [ \"$VERCEL_ENV\" == \"production\" ]; then exit 1; else exit 0; fi",
	"rewrites": [
		{
			"source": "/:path*",
			"destination": "/index.html"
		}
	]
}
