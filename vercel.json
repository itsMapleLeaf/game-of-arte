{
	"framework": null,
	"buildCommand": "convex deploy && VITE_PUBLIC_CONVEX_URL=$CONVEX_URL pnpm run build",
	"outputDirectory": "public",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/api/handler"
		}
	]
}
