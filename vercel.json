{
	"framework": null,
	"buildCommand": "convex deploy --cmd 'pnpm run build' --cmd-url-env-var-name VITE_PUBLIC_CONVEX_URL",
	"outputDirectory": "public",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/api/handler"
		}
	]
}
