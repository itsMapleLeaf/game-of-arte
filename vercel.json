{
	"framework": null,
	"buildCommand": "convex deploy --cmd 'pnpm run build' --cmd-url-env-var-name VITE_PUBLIC_CONVEX_URL --preview-run preview:seed",
	"outputDirectory": "build/client",
	"rewrites": [
		{
			"source": "/(.*)",
			"destination": "/api/handler"
		}
	]
}
